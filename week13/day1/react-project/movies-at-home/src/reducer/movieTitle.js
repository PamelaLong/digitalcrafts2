const initialState = {
    movieTitle: "",
    poster: "",

}

const movieTitle = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default movieTitle;