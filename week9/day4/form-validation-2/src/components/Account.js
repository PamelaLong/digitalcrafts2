import React from 'react'
import { AccountContainer } from '../styled-components/AccountStyle'

export default function Account() {
    return (
        <AccountContainer>
        Account
        </AccountContainer>
    )
}